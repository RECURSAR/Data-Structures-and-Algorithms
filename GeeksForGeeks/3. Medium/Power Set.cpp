#include<iostream>
#include<bits/stdc++.h>
 
using namespace std;

vector<string> AllPossibleStrings(string s) {
    
}

int main() {
    string s = "abc";
    
    vector<string> result = AllPossibleStrings(s);
    for(auto res : result)
        cout<<res<<" ";

    return 0;
}